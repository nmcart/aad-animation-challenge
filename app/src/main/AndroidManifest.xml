<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.natacha.carthias.meditate">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MeditateActivity"
            android:configChanges="uiMode" ></activity>
        <activity android:name=".InputFeelsActivity"
            android:configChanges="uiMode" />
        <activity android:name=".MeditationListActivity"
            android:configChanges="uiMode" />
        <activity android:name=".ThemeSelectionActivity"
            android:configChanges="uiMode">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>
    </application>

</manifest>